import { FirebaseApp } from "firebase/app";

/**
 * Component description.
 * 
 * @author Rohit T P
 * @returns { JSX.Element } index Component
 */
function index({app}:{app: FirebaseApp}): JSX.Element
{
    return (
        <>
            
        </>
    );
}

export default index;
